-build out user auth?


-refactor to not call new Canvas with each new render?
-fix catches
-create custom alert box
-mute peg eventlistener if peg color is already that color?
-validate unique drawing title?
